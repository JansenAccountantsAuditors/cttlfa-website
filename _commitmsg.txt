T05 step 1: extract System Health module to js/50-system.js

Moves renderSysHealth, sysHealthHtml, sysRunbookHtml and the shDT/shDate/
shAgo/shPill/shTile helpers out of the admin app block into
js/50-system.js, loaded via an ordered script tag ahead of the app block
so the function is defined before init. Self-contained: depends only on
core globals ($, sb, esc, dR) at call time. renderWebstats and LOOKER_URL
left in place (they belong to webstats). No behaviour change; the System
Health tab renders exactly as before. Revertable on its own.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LYzdhwyUpPCdjfCMzczULX
