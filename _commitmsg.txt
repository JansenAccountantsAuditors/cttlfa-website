T03: correspondence idempotency — client handling for duplicate sends

Frontend half of the duplicate-send guard. debSendOne tags a duplicate
response so the batch correspondence run counts it as "already sent
today, skipped" and marks the queue item sent, rather than a failure.
Inert until the deb-send edge-function guard is deployed (production
deploy, pending Treasurer approval).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LYzdhwyUpPCdjfCMzczULX
