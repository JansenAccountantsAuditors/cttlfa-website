T05 step 1 (redo): extract System Health to js/50-system.js via window.AC

Redone under the corrected namespace method after the first attempt broke.
System Health moves to js/50-system.js as a closure over window.AC; its
core-helper calls ($, sb, esc, dR) are repointed to A.*, the sh* helpers
and sysHealthHtml/sysRunbookHtml stay local, and the dispatcher now calls
window.AC.renderSysHealth(). The module loads after the app block so
window.AC is populated first. renderWebstats/LOOKER_URL left in place.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LYzdhwyUpPCdjfCMzczULX
