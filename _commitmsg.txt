C10 (phone): fix obligations rows overflowing on narrow phones

The obligations register rows used fixed-width flex columns that could
overflow the card on a ~360px phone. Restructured so the label and status
pill sit on one wrapping line with the statement below, no fixed widths.
Portal tab bar already scrolls horizontally and stats/grids/tables already
collapse, so no other portal mobile change needed. Access-scope half of
C10 not built (declined under D2).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LYzdhwyUpPCdjfCMzczULX
