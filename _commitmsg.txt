T05: fix System Health module — namespace param A collided with local A

Real cause of the stuck "Loading system health": sysHealthHtml has its own
local var A (= data.agent), which shadowed the closure's namespace parameter
also named A, so A.esc/A.dR resolved to data.agent.esc (undefined). Renamed
the namespace parameter to NS throughout (NS.esc/NS.dR/NS.sb/NS.$), leaving
the local A (data.agent) untouched. Bumped the cache-bust to v=3. Confirmed
in the live browser via the sys_health RPC + a direct render call.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LYzdhwyUpPCdjfCMzczULX
