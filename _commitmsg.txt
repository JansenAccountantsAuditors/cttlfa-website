T05: cache-bust js/50-system.js (?v=2)

The System Health module is correct on the server (verified: closure over
window.AC, helpers via A.*), but the browser/edge was serving the stale
broken js/50-system.js from the first attempt because the filename was
unchanged. Versioning the URL forces a fresh fetch so the new module loads.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LYzdhwyUpPCdjfCMzczULX
